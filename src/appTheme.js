import { createMuiTheme } from "@material-ui/core";
// Темы App

export const themeAppDark = createMuiTheme({
    palette: {
        type: 'dark',
    },
});

export const themeAppLight = createMuiTheme({
    palette: {
        type: 'light',
    },
})